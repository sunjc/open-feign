spring:
  application:
    name: feign
feign:
  httpclient:
    enabled: true
  client:
    default-config: default
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic